apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hrms-chart.fullname" . }}-test-connection"
  labels:
    {{- include "hrms-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hrms-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
