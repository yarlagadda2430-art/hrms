apiVersion: v1
kind: Service
metadata:
  name: {{ include "hrms-chart.fullname" . }}
  labels:
    {{- include "hrms-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hrms-chart.selectorLabels" . | nindent 4 }}
